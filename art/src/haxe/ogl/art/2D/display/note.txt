
traverse DisplayList
applyTransforms when needed, test visibility.
send data to the renderer

class Renderer
{
	passes:Vector<Material>;
	vertices:Vector<VertexStream>;
	indices:Vector<IndexStream>;
	
	public function clearall();
	
	public function render(node:DisplayObjectContainer);
}